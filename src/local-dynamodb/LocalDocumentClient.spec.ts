describe('flattenObject', () => {
  beforeAll(async () => {
    return
  })

  afterAll(() => {
    return
  })

  // it('instantiates a documentClient with defaults', () => {
  //   const documentClient = getDocumentClient()
  // })

  // it('does basic operations a local documentClient', async () => {
  //   const documentClient = getDocumentClient({
  //     local: true,
  //   })
  // })
})
